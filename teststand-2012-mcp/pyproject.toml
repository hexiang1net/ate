[project]
name = "teststand-2012-mcp"
version = "0.1.0"
description = "MCP Server for creating TestStand 2012 sequence files via ActiveX/COM API"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "pywin32>=300",
]

[project.scripts]
teststand-mcp = "mcp_server:main"
